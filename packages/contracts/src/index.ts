// Public surface of the contracts package: the types generated from the
// OpenAPI 3.1 spec. Regenerate with `pnpm --filter @manfriday/contracts gen`.
// The generated file (src/generated/schema.ts) is committed so CI can assert
// it matches the spec (the drift gate is hardened in WP 0.12).
export type { paths, components, operations } from "./generated/schema";
